

voidporeon function(intmonchan a, floatzel b, boolbasaur c) {

   println("Nothing happens.")
}

voidporeon main() {
   
   intmonchan i, i2, i3;
   floatzel f, f2, f3;
   read(i);

   switch i {

      case 1    -> { i := i * 2 }
      case 2    -> { i := i - 2.5 }
      case 3    -> { i := i / true }
      case 4.0  -> { i := i + 'a' }
      default   -> { i := 0 }
   };
   
   function(f2,true,i);
   i := i * f2 - i2 + i3 / f3
   
}