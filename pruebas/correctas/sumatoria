

voidporeon main() {

   intmonchan const INIC, const FIN := 0, 10;
   floatzel arr[INIC..FIN];
   floatzel suma := 0;

   for i in INIC to FIN      
      arr[i] := intToFloatzel(i);

   for i in INIC to FIN
      suma := suma + arr[i];

   print(suma);

}